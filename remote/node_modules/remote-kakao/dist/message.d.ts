/// <reference types="node" />
import { RemoteInfo, Socket } from 'dgram';
import { KakaoLinkClient } from 'node-kakaolink';
declare class Message {
    private socket;
    private sessionEmitter;
    private kakaoLink?;
    remoteInfo: RemoteInfo;
    room: string;
    content: string;
    sender: string;
    isGroupChat: boolean;
    getProfileImage: () => string;
    constructor(data: Record<string, any>, sessionEmitter: any, socket: Socket, remoteInfo: RemoteInfo, kakaoLink?: KakaoLinkClient);
    replyText(text: string, room?: string, address?: string, port?: number, timeout?: number): Promise<Record<string, any>>;
    replyKakaoLink(template: {
        id: number;
        args?: Record<string, any>;
    }, room?: string): Promise<unknown>;
}
export default Message;
