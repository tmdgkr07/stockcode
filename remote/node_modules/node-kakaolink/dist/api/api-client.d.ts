/**
 * Created by archethic on 2021/08/06
 */
/// <reference types="cheerio" />
import { CookieJar } from "tough-cookie";
import { AsyncLoginResult } from "../type";
export declare class ApiClient {
    #private;
    constructor(cookie: CookieJar, loginRes: cheerio.Root, apiKey: string, url: string);
    static create(apiKey: string, url: string): Promise<ApiClient>;
    login(loginParams: loginInterface): AsyncLoginResult;
}
export declare enum LoginStatusCode {
    SUCCESS = 0,
    CRYPTO_ERROR = -484,
    BLOCK_COUNTRY = -435,
    INCORRECT_ACCOUNTS = -450,
    CAPCHA_BLOCK = -481
}
interface loginInterface {
    email: string;
    password: string;
    cookieJar?: CookieJar;
    keeplogin?: boolean;
}
export {};
