/// <reference types="node" />
import { RemoteInfo, Socket } from 'dgram';
declare class Message {
    private socket;
    private sessionEmitter;
    remoteInfo: RemoteInfo;
    room: string;
    content: string;
    sender: {
        name: string;
        getProfileImage(): string;
    };
    isGroupChat: boolean;
    constructor(data: Record<string, any>, sessionEmitter: any, socket: Socket, remoteInfo: RemoteInfo);
    reply(text: string, room?: string, address?: string, port?: number, timeout?: number): Promise<Record<string, any>>;
}
export default Message;
